import { Routes } from '@angular/router';
import { DashboardOverview } from './components/dashboard-overview/dashboard-overview';

export const DASHBOARD_ROUTES: Routes = [
      { path: 'login', component: DashboardOverview },
  ];