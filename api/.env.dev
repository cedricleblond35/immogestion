# Environnement
ENV=dev

# Configuration du serveur
PORT=8080
CORS_ORIGINS=http://localhost:4201      # Autorise les requêtes CORS depuis votre frontend Angular en dev

# Configuration de la limitation de débit (rate limiting)
RATE_LIMIT_REQUESTS=1000-m    # Plus généreux en dev pour faciliter les tests
RATE_LIMIT_DURATION=m       # Limite sur 1 minute

# Configuration de la base de données Postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=immobilier_db
DB_USER=immobilier_user
DB_PASSWORD=devpassword      # Mot de passe simple pour dev (à ne pas utiliser en prod)

# Secret JWT pour les tests
JWT_SECRET=devsecret         # Clé simple pour dev (à ne pas utiliser en prod)

# Autres variables optionnelles (par exemple, Redis si utilisé)
REDIS_PASSWORD=devredispass